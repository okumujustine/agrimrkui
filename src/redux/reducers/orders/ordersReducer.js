// import { GET_PRODUCTS } from "../../actions/types";

const initialState = {
  loading: null,
};

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
